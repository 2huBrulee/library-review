/*
 * AuthorListItem Messages
 *
 * This contains all the text for the AuthorListItem component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.AuthorListItem';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the AuthorListItem component!',
  },
});
