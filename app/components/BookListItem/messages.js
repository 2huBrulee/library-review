/*
 * BookListItem Messages
 *
 * This contains all the text for the BookListItem component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.BookListItem';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the BookListItem component!',
  },
});
