/*
 * BookHandling Messages
 *
 * This contains all the text for the BookHandling component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.BookHandling';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the BookHandling component!',
  },
});
