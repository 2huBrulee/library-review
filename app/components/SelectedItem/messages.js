/*
 * SelectedItem Messages
 *
 * This contains all the text for the SelectedItem component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.SelectedItem';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the SelectedItem component!',
  },
});
